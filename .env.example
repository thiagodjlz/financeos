POSTGRES_DB=financeos
POSTGRES_USER=financeos
# Obrigatorio: gere um valor proprio (ex.: openssl rand -base64 24).
# A stack se recusa a subir sem esta variavel — nao existe senha padrao.
POSTGRES_PASSWORD=
POSTGRES_PORT=5432

BACKEND_PORT=8080
FRONTEND_PORT=80
CORS_ORIGINS=http://localhost:4200,http://127.0.0.1:4200
APP_VERSION=1.0.0
